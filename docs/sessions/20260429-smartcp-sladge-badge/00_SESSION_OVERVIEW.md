# Session Overview

## Goal

Add the sladge governance badge to the local SmartCP checkout because it is an
MCP server and AI tool orchestration surface.

## Outcome

- Added the `AI Slop Inside` badge near the top of `README.md`.
- Used isolated worktree `AgentMCP-wtrees/sladge-badge` because the canonical
  checkout is detached HEAD and warns not to push from that directory.
- Preserved the canonical checkout without local changes.

## Success Criteria

- README links to `https://sladge.net`.
- Session docs record the SmartCP/AgentMCP remote caution.
- Worktree is clean after commit.
